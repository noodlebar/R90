# Safeguards

本项目继承 `portfolio-os/global/base-policy.md`，并补充自己的项目边界。

## 项目边界

- 主目录：`R90/`
- 默认允许写入：代码、文档、测试、`artifacts/`、`tmp/`、`reports/`、`codex/`
- 默认禁止跨项目写入
- 产品定位：睡眠计划计算器和习惯辅助，不提供医学诊断、治疗或个体化医疗建议

## 高风险区域

- `.env`、部署配置、真实凭证
- 真实数据库、上传原件或导出快照
- 任何会触发正式外部副作用的脚本或目录
- 健康相关文案、免责声明、可能让用户延误就医的建议

默认规则：

- 非必要不读取敏感内容
- 读取后不回显完整密钥或敏感数据
- 不把真实密钥复制进文档、夹具或测试产物
- 不把 R90 周期计算描述成保证有效的医学结论
- 遇到严重失眠、睡眠呼吸暂停、长期疲劳等场景，应提示用户寻求专业帮助

## 默认禁止动作

- 未经明确要求直接部署、发布或同步正式外部系统
- 破坏式删除
- 直接向生产数据库做写操作
- 跳过项目自己的验证步骤

## Release 边界

本项目的 `Release` 代表：

- 形成稳定本地交付，或按明确流程执行工具类项目的正式发布

只有在用户明确要求时，才应进入 `Release`。

## 最低验证要求

至少执行一项：

- 自动化测试
- lint / type check / syntax check
- 本地 smoke test
- 针对关键脚本的最小回归

时间计算相关变更必须覆盖：

- 跨午夜倒推
- 4、5、6 cycle 默认输出
- wind-down buffer 不计入睡眠时长
- timezone 或 DST 相关行为的明确处理
