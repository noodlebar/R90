# Safeguards

本项目继承 `portfolio-os/global/base-policy.md`，并补充 R90 Agent Skill 的项目边界。

## 项目边界

- 主目录：`R90/`
- 默认允许写入：skill、脚本、文档、测试、`artifacts/`、`tmp/`、`reports/`、`codex/`
- 默认禁止跨项目写入
- 产品定位：睡眠计划计算器和习惯辅助，不提供医学诊断、治疗或个体化医疗建议

## 健康与承诺边界

- 不把 R90 周期计算描述成保证有效的医学结论。
- 不承诺用户按某个时间睡就一定清醒、恢复好或效率高。
- 遇到严重失眠、疑似睡眠呼吸暂停、长期疲劳、药物或心理健康风险时，建议寻求专业帮助。
- 默认文案应是计划建议，而不是诊断、处方或治疗方案。

## 数据边界

- 睡眠日志默认保存在本地 `~/.r90/sleep-log.json` 或 host 私有存储。
- 不默认上传、同步或分享睡眠日志。
- 不在文档、示例或测试夹具中写入真实敏感数据。
- 早晨打卡按睡眠日期 upsert，避免重复记录。

## Host Side Effects

- skill 本身不创建原生闹钟。
- 如果宿主只有 cron/chat 能力，只描述为聊天提醒。
- 外部推送、渠道权限、真实闹钟和数据同步由宿主适配层负责。
- 未经用户明确要求，不执行部署、发布、同步正式外部系统或创建真实提醒。

## 高风险区域

- `.env`、部署配置、真实凭证
- 用户真实睡眠日志
- 外部提醒、推送渠道、日历或闹钟权限
- 任何会触发正式外部副作用的脚本或目录

默认规则：

- 非必要不读取敏感内容
- 读取后不回显完整密钥或敏感数据
- 不把真实密钥复制进文档、夹具或测试产物

## 默认禁止动作

- 未经明确要求直接部署、发布或同步正式外部系统
- 破坏式删除
- 直接向生产数据库做写操作
- 跳过项目自己的验证步骤

## Release 边界

本项目的 `Release` 代表：

- 形成稳定本地交付，或按明确流程执行工具类项目的正式发布

只有在用户明确要求时，才应进入 `Release`。

## 最低验证要求

至少执行一项：

- `python3 skills/r90-sleep-planner/scripts/r90_calc.py self-test`
- 针对核心场景的 CLI smoke test
- host 侧手动测试：`我明天 8 点起`、`我现在要睡了`、`5`、`23:30-07:00`

时间计算相关变更必须覆盖：

- 跨午夜倒推
- 4、5、6 cycle 默认输出
- wind-down buffer 不计入睡眠时长
- timezone 或 DST 相关行为的明确处理
