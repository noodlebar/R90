# Agents

## Default roles

- Lead Agent: reads project memory, defines acceptance, and owns final integration
- Builder Agent: implements the requested change within a clear write scope
- Reviewer Agent: checks regressions, risks, and missing validation

## Rule

- split work by ownership, not vague function names
- define write scope before parallel work
- keep release actions owned by one agent
